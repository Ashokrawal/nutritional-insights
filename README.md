# 🥗 Nutritional Insights

A full-stack MERN application that scans food barcodes to provide instant health scores and nutritional breakdowns using the Open Food Facts API.

## 🚀 Features

- **Live Scanner:** Responsive camera-based barcode detection.
- **Health Scoring:** Custom algorithm calculating scores (0-100) based on nutrients and additives.
- **History:** Securely save and track your scan history.
- **Responsive Design:** Optimized for both mobile and desktop use.

## 🛠️ Tech Stack

- **Frontend:** React, Redux Toolkit, Tailwind CSS, Framer Motion.
- **Backend:** Node.js, Express, MongoDB, Mongoose.
- **API:** Open Food Facts.

## ⚙️ Setup

1. Clone the repo: `git clone https://github.com/Ashokrawal/nutritional-insights.git`
2. Install dependencies: `npm install`
3. Add your `.env` with `MONGODB_URI` and `PORT=5001`.
4. Run: `npm run dev`
